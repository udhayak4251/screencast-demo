services:
  socket-listener:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: socket-listener
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - ENABLE_TEST_TRIGGER=true
    restart: unless-stopped
