services:
  socket-listener:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: socket-listener
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    restart: unless-stopped
